#WACE configuration file - invalid: training with zero max_samples

logpath: "/dev/null"
loglevel: "WARN"

model_plugins:
  - id: "trivial_training"
    plugin_type: RequestHeaders
    path: "testdata/plugins/trivial.so"
    training: true

decision_plugins:
  - id: "weighted_sum"
    path: "testdata/plugins/weighted_sum.so"
    waf_weight: 0.5
    params:
      threshold: "0.5"

crs_version: "4.4.0-dev"

exception_ids:
  RequestHeaders: 100
