#WACE configuration file - invalid: training + async combination

logpath: "/dev/null"
loglevel: "WARN"

modelplugins:
  - id: "trivial_training"
    plugintype: RequestHeaders
    path: "testdata/plugins/trivial.so"
    weight: 0.25
    training: true
    async: true
    training_data:
      max_samples: 10
      result_file_path: "/tmp/training_results.json"

decisionplugins:
  - id: "weighted_sum"
    path: "testdata/plugins/weighted_sum.so"
    params:
      waf_weight: "0.5"
      threshold: "0.5"

crs_version: "4.4.0-dev"

exception_ids:
  RequestHeaders: 100
